
<div class="container">
 <div class="row">
   <div class="mx-auto col-11 col-sm-10 col-lg-8">
     <h4>ログイン</h4>

<h5>会員の方はこちらからログイン</h5>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <table class="field">
    <tr>
    <th><%= f.label :メールアドレス %></th>
    <th><%= f.email_field :email, autofocus: true, autocomplete: "email" %></th>
    </tr>
  </table>

  <table class="field">
    <tr>
    <th><%= f.label :パスワード %></th>
    <th><%= f.password_field :password, autocomplete: "current-password" %></th>
  </table>

  <div class="actions">
     <button class="btn btn-success" type="submit">ログイン</button>
  </div>
<% end %>

<h4>会員登録がお済みでない方</h4>
<h5><%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください。</h5>
</div>
</div>
</div>