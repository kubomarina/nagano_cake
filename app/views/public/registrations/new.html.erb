<h3>新規登録</h3>
<div class="container">
 <div class="row">
   <div class="mx-auto col-11 col-sm-10 col-lg-8">
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>


  <table class="field">
   <tr>
    <th><%= f.label :名前 %></th>
    <th>(姓)<%= f.text_field :last_name, autofocus: true %></th>
    <th>(名)<%= f.text_field :first_name, autofocus: true %></th>
   </tr>

  <tr>
    <th><%= f.label :フリガナ %></th>
    <th>(セイ)<%= f.text_field :last_name_kana, autofocus: true %></th>
    <th>(メイ)<%= f.text_field :first_name_kana, autofocus: true %></th>
  </tr>

  <tr>
    <th><%= f.label :メールアドレス %></th>
    <th><%= f.email_field :email, autofocus: true, autocomplete: "email" %></th>
  </tr>

  <tr>
    <th><%= f.label :郵便番号（ハイフンなし）%></th>
    <th><%= f.text_field :postal_code, autofocus: true %></th>
  </tr>

  <tr>
    <th><%= f.label :住所 %></th>
    <th><%= f.text_field :address, autofocus: true %></th>
  </tr>

  <tr>
    <th><%= f.label :電話番号（ハイフンなし） %></th>
    <th><%= f.text_field :telephone_number, autofocus: true %></th>
  </tr>

  <tr>
    <th><%= f.label :パスワード %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> 文字以上)</em>
    <% end %>
    </th>
    <th><%= f.password_field :password, autocomplete: "new-password" %></th>
  </tr>

  <tr>
    <th><%= f.label :パスワード（確認用） %></th>
    <th><%= f.password_field :password_confirmation, autocomplete: "new-password" %></th>
  </tr>
</table>

  <div class="actions">
   <button class="btn btn-success" type="submit">新規登録</button>
  </div>
  <% end %>

<h4>すでに登録済みの方</h4>
<h5><%= link_to "こちら", new_customer_session_path %>から新規登録してください。</h5>
</div>
</div>
</div>